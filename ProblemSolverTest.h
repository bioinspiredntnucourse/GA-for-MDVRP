#pragma once
/*
To be made into a test if wanted

cout << "init population" << endl;
for (unsigned int i = 0; i < population.size(); i++) {
	cout << population[i].fitness << endl;
	//DrawSolutionInstance(this->problem, population[i]);
}

population.insert(population.end(), population.begin(), population.end());

cout << endl << "extended population" << endl;
for (unsigned int i = 0; i < population.size(); i++) {
	cout << population[i].fitness << endl;
	//DrawSolutionInstance(this->problem, population[i]);
}

population = SelectNextGeneration(population);

cout << endl << "result population" << endl;
for (unsigned int i = 0; i < population.size(); i++) {
	cout << population[i].fitness << endl;
	//DrawSolutionInstance(this->problem, population[i]);
}
*/